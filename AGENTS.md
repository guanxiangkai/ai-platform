# ai-platform

当前代码和数据模型定义为 1.0.0 首个正式基线。源码、注释、测试和配置只保留当前有效模型，不建立并行兼容契约或描述不存在的版本演进。公开仓库不保留额外部署目录、额外文档目录、数据库脚本或种子数据。

跨服务重复的安全、租户、数据、缓存、Web 和前端能力必须分别下沉到 AI Plus 或 AI UI；平台仓库仅保留平台领域组合。设计模式用于真实变化点，不为凑齐模式数量增加空抽象。公开 API 和关键边界统一使用准确的中文 Javadoc。

`platform-agent` 是跨应用、跨租户的通用独立服务，和网关、认证、系统、文件、SSE 一样由本仓库部署。它不得依赖任何消费方模块、业务数据库、私有配置或硬编码默认智能体。

Agent 数据与平台数据使用由部署环境提供的数据源，并通过模块前缀划分表所有权；租户数据通过 `DataTenantEntity` 的 `tenant_id` 实施行级隔离。消费方只能经共享网关访问平台能力，不得在业务数据库复制 Agent、Skill、Knowledge 或 SSE 定义与记录。

所有运行地址、命名空间、配置组、数据库、Redis、对象存储、消息系统和第三方凭据必须由部署环境注入；源码、构建资源、测试和示例不得包含真实连接信息或秘密值。根 `docker/` 仅提供无真实数据的参数化示例，公开仓库不分发任何环境的 Nacos 配置、数据库连接或种子数据。

所有 Java 模块的编译工具链和容器运行时统一使用 Oracle GraalVM 25.0.4；容器直接使用官方 `container-registry.oracle.com/graalvm/jdk:25.0.4` 基础镜像，并在构建时强制拉取。

`platform-scheduler` 是通用调度控制面，PowerJob Server 是其独立容器内的第三方调度引擎，两者由根 `docker/docker-compose.yml` 统一编排但保持进程和镜像隔离。任务处理器按领域归属嵌入消费方服务，业务逻辑不得进入通用调度服务。

当前公开基线只维护受保护的 `main`；构建和测试在 GitHub 托管的 Linux CI 环境执行，后续修改通过功能分支、Pull Request 和必需状态检查进入 `main`，不得向固定内网主机同步源码。
